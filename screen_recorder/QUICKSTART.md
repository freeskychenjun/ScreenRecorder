# 快速开始指南

## 第一次使用

### 1. 安装依赖

打开命令提示符或PowerShell，进入项目目录：

```bash
cd screen_recorder
pip install -r requirements.txt
```

### 2. 测试安装（可选）

运行测试脚本验证所有模块正常工作：

```bash
python test_recorder.py
```

你应该看到所有测试通过的消息。

### 3. 启动程序

```bash
python main.py
```

## 基础使用

### 全屏录制（最简单）

1. 启动程序后，直接点击 **"▶ 开始录制"** 按钮
2. 程序会使用默认设置（全屏、30FPS、H264编码）
3. 录制完成后点击 **"⏹ 停止"** 按钮
4. 视频会自动保存到桌面的 `Recordings` 文件夹

### 自定义区域录制

1. 点击 **"📷 选择录制区域"** 按钮
2. 屏幕会出现一个半透明的覆盖窗口
3. 用鼠标拖拽出你想要的录制区域
4. 点击 **"确认选择"**
5. 点击 **"▶ 开始录制"** 开始录制选定的区域

### 使用预设分辨率

在区域选择器中，从下拉菜单选择：
- **1920x1080** - Full HD
- **1280x720** - HD
- **854x480** - SD
- **640x360** - 低分辨率

## 调整录制质量

### 提高画质
- 增加 **帧率** 到 60 FPS
- 使用 **H264** 编码器
- 选择较大的录制区域

### 减小文件大小
- 降低 **帧率** 到 15-30 FPS
- 选择较小的录制区域
- 避免录制静态内容时间过长

## 录制技巧

### 游戏录制
- 使用 60 FPS
- 选择 **H264** 编码器
- 只录制游戏窗口区域，不要全屏

### 软件演示
- 使用 30 FPS（足够流畅）
- 可以使用 15-30 FPS 以减小文件大小
- 考虑使用标注工具（开发中）

### 教程录制
- 30 FPS 是最佳选择
- 全屏录制可以显示更多细节
- 使用清晰的鼠标移动

## 常见问题

### Q: 录制在哪保存？
A: 默认保存在 `C:\Users\你的用户名\Desktop\Recordings`，可以在界面中更改。

### Q: 如何暂停录制？
A: 点击 **"⏸ 暂停"** 按钮，再次点击继续录制。

### Q: 录制的视频很大？
A: 尝试降低帧率或使用H264编码器。

### Q: 程序无法启动？
A: 确保已安装所有依赖：`pip install -r requirements.txt`

## 下一步

- 查看 [README.md](README.md) 了解更多高级功能
- 运行 `python test_recorder.py` 测试系统性能
- 尝试不同的编码器和设置组合

## 需要帮助？

如果遇到问题：
1. 检查是否安装了所有依赖
2. 运行测试脚本查看错误信息
3. 确保 Python 版本 >= 3.8
